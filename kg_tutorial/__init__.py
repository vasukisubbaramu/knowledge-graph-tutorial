"""Shared utilities for the KG tutorial.

Import surface:

    from kg_tutorial import config, llm, embed, display
    from kg_tutorial.data import schema, generate

Everything is kept small and inspectable on purpose. If you find yourself
asking "what does this do," open the file — it should fit on one screen.
"""

__version__ = "0.1.0"
